# Relay Code 北极星愿景（VISION v2）

> 状态：愿景对齐 v2.1 · 2026-08-01（v2 重写；v2.1 补充呈现层与集群 App / mod 模式）
> 定位：战略锚点（为什么做）；ROADMAP.md = 执行路线（做什么）；docs/framework-design.md = 系统设计（怎么做）。
> 版本说明：v2 基于多轮愿景对齐对话重写。核心修正：能力挂载化、设计即服务、编程化控制流、建造者视角、集群为服务主体。以最深最清晰的理解为准。

---

## 一、定位：一个 harness，一个新范式

**一句话北极星**：

> Relay-Code 是一个 **harness**——允许用户把 agent 封装成服务、挂载为进程，组成一个异构 agent 集群；用户用编程化控制流编排集群，系统忠实执行用户的设计。**集群是面向用户的单一服务主体。**

**范式四要素**：

1. **agent 即服务**：能力由人封装挂载，不是框架承诺。每个 agent 的 prompt、API 模型、工具模型、harness、skill 都可以不同——节点完全异构，框架不假设任何共性之外的内部结构。
2. **编程化控制流**：用户像编程一样编排集群——循环（loop）、分派（switch）、门控（if）、管道（pipe）、并行聚合（fan-out/merge）、定时（timer）——控制 agent 的输出与行为。决策点只是控制流里的一个 `if`。
3. **集群可扩展**：新 agent 服务随时加入集群，扩展服务边界，无需改框架本体。
4. **集群即服务主体**：用户消费的是一个集群，不是零散 agent。主 agent 是集群里的一个推理节点（有集群视野），不是独立于集群的舵手。

**呈现形态（headless 核心 + 多前端）**：
- harness 核心是**无界面的 daemon**（headless），CLI 与 Web 都是它的客户端；
- **Web 是集群的主界面**（节点/流/门控/状态/审计多视图），CLI 降级为建造者的调试通道（可脚本化、可 headless 操作）；
- 集群是 7×24 服务——用户关掉终端，它仍在运转。

**消费形态（集群即 App · mod 模式）**：
- 集群服务像一个游戏，agent 服务像**插件/mod**——多个 mod 共同作用让游戏更好玩，集群也因服务组合而增值；
- C 端用户不写 Flow、不拖节点，只需**插入加载**打包好的集群 app（求职集群、套利集群、学习集群）；
- 我们做底层，必须支持这条加载链路：**打包、签名校验、契约校验、依赖解析、安装、热加载、版本管理**；
- 编排能力分层：建造者（文本 DSL + 代码）→ Pro 用户（低代码拖拽编辑器——Flow 是数据，编辑器只是壳）→ 终端用户（安装成品，零编排）。

**诚实条款（边界先于功能声明）**：

- **能力是挂载的，不是承诺的**——服务必须诚实汇报"我做不到"，主 agent 据此换路，而不是让服务硬编；
- **信息源是积累的，不是凭空获取的**——输入侧来源靠用户投喂 + 服务经验积累（源信誉），不期待模型凭空获取；
- **系统不自动分解目标**——用户设计几个维度、配几组工具，就有几路服务。"自动分解"是主 agent 生成服务的远期能力（复制用户设计），不是运行时魔法；
- **北极星是方向，不是验收清单**——功能很多，不一定全部做到；ROADMAP 分批交付，每阶段可运行、可验证。

**渐进路线（决定开发顺序）**：

手动装配（用户定义服务，Phase 1-2）→ 主 agent 生成（Phase 3+，学习用户设计后复制）→ 生态安装（最后）。
第一版的面貌是"**人装服务的骨架 + 控制流**"，不是"一句话自动多集群"。

---

## 二、示例扩展（剧本演示，非北极星本体）

> 本节是示例。目的：展示框架机制如何实例化为具体应用。任何领域 = 定义若干服务 + 维度模型 + 控制流，全部以配置进入框架，无需改框架本体。

### 示例：求职 · 学习 agent 开发

1. **用户设计集群**：目标"求职 AI Agent 开发岗"，拆成 6 个维度（基础 / 项目 / 八股 / LeetCode / 简历 / 情报）；每维度配一个服务（自动老师、题目收集器、简历器、JD 监控）；定义节奏与控制流（每天学几小时、什么时候检测、什么条件触发通知）。
2. **集群按设计运转**：学习按节奏推进——基础 → 项目 → 八股 → LeetCode；项目里遇到的知识点自动归入八股笔记；内容交叉处自动关联（维度间共享上下文）。
3. **监控型服务常态 silent**：JD 监控一天检测 142 次，142 次"无发现"被吸收；只有技能要求变化才触发门控。
4. **效率感知**：答题错误率上升 → 效率事件 → 门控规则建议切换维度或休息。
5. **用户隔天没来**：推进型服务照常整理笔记、更新简历草稿；用户回来收到摘要"你不在期间：技能缺口 X、简历更新建议、3 道新面试题"。
6. **用户只在建造与拍板时介入**：调整维度/节奏/控制流（他是建造者），或对门控触发的事件做决策。

### 通用服务原型（框架层抽象，任何领域通用）

按"触发源 × 行为"两个维度完备覆盖：

| | 执行（做一件推进的事） | 观察（检测、判断） | 应答（基于上下文回答） |
|---|---|---|---|
| **时间触发**（周期/定时） | **推进型**：自动老师。按节奏推进维度，产出工件（笔记、草稿、报告） | **监控型**：套利/JD 监控。循环检测，常态 silent，越阈值才发声 | （少见） |
| **外部触发**（用户/事件） | （少见） | （少见） | **交互型**：答疑老师。被问才答，但记得之前教到哪（应答但连续，不失忆） |

其余组合归为 hybrid（多格混用，如教学服务既推进又交互）。三分不是拍脑袋，是触发 × 行为的完备覆盖。

---

## 三、范式优点

1. **注意力反转**：默认不打扰——**宁可漏报（信息进摘要、可回补），不可误报（打扰信任崩塌、不可回补）**。系统替用户盯世界，只在用户设计的门控条件满足时开口。
2. **并行度脱离串行注意力**：N 个服务并行推进，用户只接一路。人类靠一轮轮对话能获取的服务量有上限，这个架构拆掉了瓶颈。
3. **从"问"到"建造"**：旧范式每次输入 prompt 才能得到服务；新范式**设计一次、永久运转**——用户定义服务与控制流，执行、调度、过滤全自动。
4. **上下文资产化**：集群级记忆跨服务累积（进度、目标、习惯、源信誉）。了解越深 → 门控判断越准 → 打扰越少但越值钱 → 正反馈。
5. **服务可关联**：节点共享事件/上下文总线。学习发现的知识可以喂给套利分析，套利事件可以反过来调整学习节奏。
6. **从"求解"到"推进"**：旧心智模型是"任务 = 单次求解"；真实成事（求职、健身、赚钱）是多维度的长期状态。价值单位从"完成的任务"变成"被推高的维度"。
7. **插件化消费（mod 模式）**：集群如游戏、服务如 mod——C 端用户零编排、插入加载即可获得完整服务；生态即产品边界，能力随安装增长。

---

## 四、架构支柱（原则）

### 支柱 1：能力挂载（agent 即服务）
- 框架只写两件事：底层原子工具的**适配层**（先天写入）+ **挂载机制**（后天可插：MCP / 外部能力 / 自定义工具模型）。
- 节点完全异构：prompt / model / tools / execution / skills 各不相同——编程 agent 挂 shell，视频 agent 挂多模态工具。
- 服务契约是节点的"自我声明"（能干什么、发什么事件、要什么权限），主 agent 据此发现与调度。

### 支柱 2：设计即服务（忠实执行）
- 框架**不自动分解目标**。用户设计几个维度、配几组工具 → 系统提供几路服务。设计是唯一的输入，行为全部可回溯到设计。
- "主 agent 生成服务"（Phase 3+）是用户设计的**自动化复制**，不是系统自主发明——生成样本是用户已定义的服务。

### 支柱 3：编程化控制流（可塑造 + 可稳定控制）
- 控制流原语：`if`（门控）/ `loop`（循环）/ `switch`（分派）/ `pipe`（顺序）/ `fan-out+merge`（并行聚合）/ `timer`（节奏）。
- 控制流操控节点的**输出与行为**——过滤、改写、路由、合并、触发，agent 是数据流里的处理节点。
- **可稳定被控制是特化的前提**：标准协议、确定的行为边界（权限/超时/重试）、可见的状态（可被条件判断）、可处理的错误（流挂了可修）。**不可预测的 agent 无法被编排，只能被"哄"。**

### 支柱 4：建造者视角（可维护 = 可观测）
- 用户是**建造者与维护者**，不是掌舵者——所有控制流都由用户建立，主 agent 只是控制流里的推理节点。
- 框架的第一类体验是**调试器**而不是对话：状态可查、事件可溯、控制流可验证。Web 面板是控制台，不是通知面板。

### 支柱 5：目标维度与持续推进（时间感知）
- **维度 ≠ 任务**：任务做完消失；维度是持续推高的状态（基础掌握度、简历完善度）。
- 服务在时间轴上**主动推进**，不为"用户在线"运行，为"维度需要"运行。
- 时间模型：效率感知（答题变慢/错误率上升）、节奏切换、间隔复习（隔一两天主动整理）。

### 支柱 6：信任、权限与守护（运转底线）
- **信任**：影响决策的事件二次验证（`requiresVerification`）+ 全量审计；服务诚实汇报能力边界。
- **权限**：服务声明权限（文件/网络/工具），框架强制——常驻自主服务的刹车片。
- **守护**：心跳、崩溃退避重启、启动恢复——"7×24 常驻"承诺的地基，不显式列入架构就会没人做。

---

## 五、集群分层架构

```
┌─ 呈现层（多前端 · 核心无头）──────────────────────────┐
│  Web 主界面 · CLI 调试通道 ·（未来）桌面/移动           │
├─ 控制流层（用户编程）────────────────────────────────┤
│  if / loop / switch / pipe / fan-out / timer         │
│  操控节点的输出与行为 · 门控 = 用户规则 + LLM 兜底       │
├─ 节点群（异构 · agent 为颗粒度）─────────────────────┤
│  [主 agent]  [推进型×N]  [监控型×N]  [交互型×N]  ...  │
│  prompt / model / tools / execution / skills 各异    │
├─ 框架机制（我们做的）────────────────────────────────┤
│  协议（唯一共性）· 生命周期 · 编排 · 观测               │
└────────────────────────────────────────────────────┘
```

**框架只承诺四件事**：协议（指令/事件统一通道）、生命周期（挂载、守护、重启）、编排（控制流驱动节点）、观测（状态可查、事件可溯）。其余全部是节点的配置——框架越薄，集群的扩展边界越大。

---

## 六、现状差距分析（2026-07-31）

### 已具备（骨架）
| 能力 | 现状 |
|---|---|
| 挂载进程通道 | ✅ `actor.ts` + `actor-handle.ts` — stdin/stdout JSONL、pending-promise 机制 |
| 事件入口 | ✅ `inbox.ts` |
| 事件总线（UI 广播） | ✅ `sink.ts`（MultiSink） |
| 节点状态分层 | ✅ `agent-registry.ts` — L1 快照 / L2 peek / L3 对话 |
| Web 面板（雏形） | ✅ `server.ts`（HTTP + WebSocket） |
| 主 agent 事件循环 | ✅ `orchestrator.ts`（daemon 模式） |
| 多轮对话记忆 | ✅ `talk` 多轮记忆、JSONL 持久化 |

### 缺失（北极星核心）
| 能力 | 缺口 |
|---|---|
| **统一事件协议** | 三个词表（Actor/Sink/AgentEvent）互不相通，无分级（silent/notify...） |
| **服务契约与挂载** | 无契约概念；`allowed_tools` 只是从框架工具表勾选，无工具模型/execution/权限声明 |
| **控制流层（Flow）** | inbox 只做简单分类，无 if/loop/switch/pipe 等可编程原语 |
| **门控决策** | 无通知策略，`agent_done` 直接打印；无默认不打扰、无反馈沉淀 |
| **生命周期守护** | actor 退出只打日志——无心跳、无退避重启、无启动恢复 |
| **全局状态模型** | registry 只有运行态，无 L0 结构化状态、无目标维度 |
| **时间感知** | 无 Scheduler、无效率检测 |
| **观测控制台** | Web 面板只显示事件流，无状态查询、无控制流可视化、无审计检索 |
| **集群 App 加载链路** | 无服务包格式、无签名/依赖校验——C 端"插入加载"未建 |
| **权限 enforcement** | ToolExecutor 无权限过滤 |

---

## 七、阶段规划

| 阶段 | 主题 | 内容 | 对应缺口 |
|---|---|---|---|
| **Phase 1** | 骨架与手动装配 | 统一事件协议 + 服务契约 + 运行时/守护 + 最小控制流（loop/trigger/if）；**人装两个示例服务跑通** | 协议 / 契约 / 守护 / Flow 最小集 |
| **Phase 2** | 知晓与开口 | StateStore（L0-L3）+ `query_state` 工具 + 门控决策（规则优先、反馈沉淀、默认不打扰）+ 时间感知 | 状态模型 / 门控 / 时间 |
| **Phase 3** | 创生与装配 | 主 agent 生成服务（复制用户设计）+ 生态安装 + 契约校验/热加载 | 创生 |
| **Phase 4** | 关联与底线 | 关联层（规则预筛 + LLM 精判）+ 权限 enforcement + 守护强化 | 关联 / 权限 |
| **Phase 5** | 生态（mod 模式） | 集群 App 市场：打包 / 签名 / 依赖 / 安装 / 热加载 / 版本（见 framework-design §10.2）+ 低代码编辑器（可选壳） | 生态 / 加载链路 |

---

## 八、与现有 ROADMAP 的关系

- VISION 回答"**为什么**"，ROADMAP 回答"**做什么、何时**"，framework-design 回答"**怎么做**"。
- ROADMAP v0.2.0 剩余项（命令注入白名单、路径穿越校验、Mock 模式、CLI `--help/--version`）属于工程收尾，不阻挡主线，可随时穿插。
- ROADMAP v0.3/v0.4 项与愿景阶段对齐：
  - Docker 沙箱 → Phase 4 权限底线
  - 多供应商支持 → 节点级 `model` 字段（框架天然支持异构模型）
  - 会话持久化/恢复 → Phase 2 之前应完成的底线（StateStore 落盘的延伸）
  - 插件系统 / 社区模板市场 → Phase 5 集群 App 市场（mod 模式）

---

*本文档随开发推进持续修订；每次架构决策应能回答"它服务于哪根支柱"。*
